1.Resolve locally and then resolve remotely
2.Draw as much linked list as you can
3.node with 2 pointers 2 way merge
4.all nodes with single pointer : fast forward startergy
5.No dirty state so commit frequently
